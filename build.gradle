rootProject.version = "10.0.alpha1"
group = 'javasabr.rlib'

allprojects {
  repositories {
    mavenCentral()
  }
}

wrapper {
  gradleVersion = '9.0.0'
  distributionType = Wrapper.DistributionType.ALL
}
