plugins {
  id("configure-java")
  id("configure-publishing")
}

dependencies {
  compileOnly projects.rlibReusable
  testImplementation projects.rlibLoggerImpl
  testImplementation projects.rlibReusable
}